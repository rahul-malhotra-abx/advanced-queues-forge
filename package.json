{
  "name": "advanced-queues-forge",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.js",
  "scripts": {
    "ui:install": "cd static/forge-angular-app && npm install --legacy-peer-deps",
    "ui:build": "cd static/forge-angular-app && npx ng build --configuration production --base-href ./ --deploy-url ./",
    "ui:watch": "cd static/forge-angular-app && npx ng build --watch --configuration development --base-href ./ --deploy-url ./",
    "deploy": "npm run ui:build && forge deploy",
    "forge:upgrade": "forge install --upgrade"
  },
  "dependencies": {
    "@forge/api": "^5.1.0",
    "@forge/kvs": "^2.0.4",
    "@forge/resolver": "^1.7.0"
  }
}
